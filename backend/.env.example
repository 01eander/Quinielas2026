# Desarrollo local (frontend Vite en 5173 + backend API)
PORT=3001
SERVE_FRONTEND=false
HOST=0.0.0.0

DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=YourPassword123!
DB_NAME=QuinielaMundial2026

JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

CORS_ORIGIN=http://localhost:5173,http://172.16.105.154:5173
LAN_IP=172.16.105.154

# URL pública de la app (enlaces de restablecer contraseña)
APP_URL=http://localhost:5173

# Correo (opcional en dev; en producción configurar para "olvidé mi contraseña")
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=quiniela@tuempresa.com
# SMTP_PASSWORD=
# SMTP_FROM=Quiniela 2026 <quiniela@tuempresa.com>

# Producción sin IIS (Node sirve web + API en un solo puerto):
# PORT=8080
# SERVE_FRONTEND=true
# CORS_ORIGIN=http://MX-QRO-APP1:8080,http://192.168.X.X:8080
